import axios from 'axios';

/**
 * Service for Matterport 360 integration
 */
export const syncM360Tour = async (developmentId) => {
  console.log(`Syncing Matterport 360 tour for ${developmentId}`);
};
